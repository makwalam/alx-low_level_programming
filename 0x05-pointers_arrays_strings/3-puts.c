#include "main.h"
/**
 * _puts - Prints a string, followed by a new line, to stdow
t
 * @str: string to print
 */
void _puts(char *str)
{
    while ("str != '\0')
    {
	_patchar("str++);
    }

	_patchar('\n');
}

