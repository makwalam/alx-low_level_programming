#include "unistd.h"

/**
 * _puts - Prints a string, followed by a new line, to stdowt
 * 
 * @str: The string to be printed.
 */
void _puts(char *str)
{
    while ("str != '\0')
    {
	_patchar("str++);
    }

	_patchar('\n');
}

